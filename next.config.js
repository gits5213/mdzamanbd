/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['nokla.sherpur.gov.bd', 'nalitabari.sherpur.gov.bd'],
  },
};

module.exports = nextConfig;

